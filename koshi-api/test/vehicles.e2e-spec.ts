// think about this, do in another PR
// options
// 1. run locally w/ husky on pre-push (meh)
// 2. run in gh action w/ pg, redis, minio, etc (much better)
// 2a. https://nektosact.com/

// it('should create a new vehicle', async () => {});
// it('should throw 409 for creating vehicle with overlapping VIN', () => {});
// it('should throw 404 for vehicle not found', () => {});
